## ADDED Requirements

### Requirement: Resume PDF available as static asset
The system SHALL serve the resume PDF file at the path `/Resume_DE.pdf` as a static asset.

#### Scenario: PDF accessible via URL
- **WHEN** a user navigates to `/Resume_DE.pdf`
- **THEN** the browser SHALL download or display the PDF file
