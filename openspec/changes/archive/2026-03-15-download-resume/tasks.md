## 1. Static Asset Setup

- [x] 1.1 Copy `Resume_DE.pdf` to `static/Resume_DE.pdf`

## 2. About Page Update

- [x] 2.1 Replace the "Download Resume" `<button>` with an `<a>` element, set `href="/Resume_DE.pdf"` and add `download` attribute
